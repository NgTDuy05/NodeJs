class Category {
    constructor(id, name, slug, createdAt) {
        this.id = id;
        this.name = name;
        this.slug = slug;
        this.createdAt = createdAt;
    }
}

module.exports = Category;
